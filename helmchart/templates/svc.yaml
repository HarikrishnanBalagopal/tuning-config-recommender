{{- if .Values.svc }}
apiVersion: v1
kind: Service
metadata:
  name: tcr-svc
  labels:
    system/internal: tuning-config-recommender-any
spec:
  selector:
    system/internal: tuning-config-recommender-any
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  type: ClusterIP
{{- end}}